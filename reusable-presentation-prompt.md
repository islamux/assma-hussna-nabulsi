# برومبت تجهيز عرض تقني احترافي للمشاريع

هذا برومبت عام يمكن استخدامه مع أي مشروع تريد تقديمه أمام فريق تقني يريد التأكد من أنك تفهم الكود والقرارات الهندسية، وليس فقط الواجهة النهائية.

## طريقة الاستخدام

استبدل القيم بين الأقواس قبل إرسال البرومبت:

- `[اسم المشروع]`
- `[مسار المشروع]`
- `[لغة العرض]`
- `[مدة العرض]`
- `[نوع الجمهور]`
- `[مجلد المخرجات]`

## البرومبت

````text
أنت مهندس برمجيات Senior، ومصمم عروض تقنية، ومراجع توثيق دقيق.

أريد منك تجهيز حزمة عرض احترافية وإبداعية للمشروع التالي:

- اسم المشروع: [اسم المشروع]
- مسار المشروع: [مسار المشروع]
- لغة العرض: [لغة العرض]
- مدة العرض: [مدة العرض]
- الجمهور: [نوع الجمهور]
- مجلد المخرجات: [مجلد المخرجات، والافتراضي presentation/]

الهدف ليس الترويج للمشروع فقط، بل إثبات أن صاحب المشروع يفهم ما كُتب فعلاً: كيف تنتقل البيانات، لماذا اختيرت كل تقنية، ما حدود كل قرار، وكيف يمكن اختبار النظام وتحسينه.

## قواعد العمل الإلزامية

1. اقرأ ملفات تعليمات المشروع أولاً مثل AGENTS.md وCLAUDE.md وREADME.md وأي دليل أسلوب موجود.
2. افحص حالة Git الحالية قبل التعديل. أنشئ فرعاً مستقلاً باسم وصفي يبدأ بـ docs/ أو presentation/، ولا تحذف أو تعكس تغييرات موجودة لم تنشئها.
3. استكشف المشروع من المصدر الفعلي، ولا تعتمد على تخمينات مبنية على اسم الإطار أو بنية شائعة.
4. قبل الكتابة، حدّد نوع المهمة: هل هي حزمة عرض محدودة أم تحتاج تصميم معماري أكبر؟ اسأل سؤالاً واحداً في كل مرة فقط إذا بقي أحد الحقول بين الأقواس غير معبأ أو وُجد غموض فعلي في المتطلبات؛ لا تسأل عمّا هو معطى أصلاً.
5. حصر حالات انتظار الموافقة: اعرض مقاربتين أو ثلاثاً مع المفاضلات والتوصية وانتظر موافقة المستخدم فقط عند اختيار الهوية البصرية لـ slides.html أو عند وجود بنى متباينة جوهرياً للحزمة؛ فيما عدا ذلك نفّذ مباشرة وفق هذا البرومبت.
6. لا تدّع وجود function أو endpoint أو config key أو test أو ميزة لا تجدها في المصدر. تحقق من كل اسم ومسار ورقم وسطر قبل توثيقه.
7. لا تعدّل كود التطبيق أو تضف dependencies إلا إذا طلب المستخدم ذلك صراحة. الحزمة المطلوبة توثيقية وعرضية، ويجب أن تكون مستقلة قدر الإمكان.
8. لا تستخدم لغة تسويقية عامة مثل «سريع جداً» أو «آمن بالكامل» بلا benchmark أو دليل من المشروع. اذكر القيود والمقايضات بوضوح؛ معرفة ما لم يُحل دليل قوي على الفهم.
9. اجعل النص موجهاً للجمهور المحدد، واستخدم [لغة العرض] للنصوص. احتفظ بأسماء الأدوات والمفاهيم البرمجية بالإنجليزية عندما يزيد ذلك الدقة، مثل SSG وPWA وHydration وRoute Handler.

## مرحلة فهم المشروع

أنشئ ملخصاً داخلياً منظماً قبل بناء الملفات يتضمن:

- نوع المنتج والمشكلة التي يحلها.
- stack وإصداراته من package.json أو ملفات المشروع.
- خريطة المجلدات ونقاط دخول التطبيق.
- routing والصفحات العامة والصفحات الديناميكية.
- Server/Client boundaries إن كان المشروع يستخدم SSR أو SSG أو RSC.
- state management ومصادر الحالة المحلية والسحابية.
- data model وdata flow من المصدر إلى الواجهة.
- API endpoints، authentication أو identity model، وقاعدة البيانات.
- build pipeline وscripts وPWA أو caching إن وجدت.
- testing strategy وعدد الاختبارات الفعلي من نتيجة test runner.
- أكبر قرارات الأداء والأمان وقابلية التوسع.
- القيود المعروفة والمخاطر والميزات غير الموجودة.
- إن لم توجد اختبارات أو أرقام أداء فعلية فسجّل ذلك كقيد صريح في كل المواد، ولا تختلق أرقاماً أو تغطية اختبارية.

استخدم مراجع دقيقة بصيغة `path/to/file.ts:line-range` في المواد، وافتح المصدر للتحقق من الأرقام قبل اعتمادها.

## الحزمة التي يجب إنشاؤها

أنشئ داخل `[مجلد المخرجات]` الملفات التالية، ما لم يطلب المستخدم خلاف ذلك:

### 1. `slides.html`

أنشئ عرض HTML مستقلاً يعمل مباشرة في المتصفح ولا يحتاج build إضافياً أو dependency جديداً.

المتطلبات:

- RTL إذا كانت لغة العرض عربية، مع `lang` و`dir` صحيحين.
- هوية بصرية مميزة نابعة من مجال المشروع، لا قالب بطاقات عام أو ألوان عشوائية.
- استخدم نظام ألوان صغيراً، خط عرض واضحاً، خط نص مقروء، وخطاً تقنياً للكود عند الحاجة.
- اجعل الملف مستقلاً تماماً عن الشبكة: لا روابط CDN لخطوط أو مكتبات أو أيقونات؛ استخدم system font stack أو خطوطاً مضمّنة base64. العرض الحي لا يعتمد على الإنترنت.
- اختر عنصراً بصرياً مميزاً واحداً يتكرر باعتدال، مثل مسار بيانات أو هامش تحريري أو شبكة زمنية.
- لا تضع نصاً كثيفاً بلا hierarchy. اجعل كل شريحة تجيب سؤالاً واحداً.
- أضف تنقلاً بالكيبورد باختصارات موحدة: الأسهم وSpace للتنقل، Home/End للانتقال إلى الطرفين، F لملء الشاشة، N لملاحظات المتحدث، O لعرض الفهرس، Esc للإغلاق. وثّق الاختصارات نفسها في README. أضف أزراراً ذات `aria-label`، فهرس شرائح، ملاحظات للمتحدث، ودعم اللمس إن كان مناسباً.
- احترم `prefers-reduced-motion`، وأضف responsive styles للهاتف، وprint styles إذا كان ذلك مفيداً.
- اجعل ترقيم الشرائح ديناميكياً أو تحقق آلياً من أنه يطابق العدد الحقيقي.
- استخدم مقتطفات كود مطابقة للمشروع مع HTML-escaping كامل عند تضمينها في slides.html. إذا كان المقتطف غير كامل، سمّه صراحة «مقتطف» ولا تعرض كوداً وهمياً كأنه قابل للتشغيل.

قسّم العرض إلى قصة واضحة تناسب `[مدة العرض]`، بمعادلة إرشادية: شريحة إلى شريحتين لكل دقيقتين من زمن الكلام، بعد خصم وقت الديمو والأسئلة:

- الافتتاح: ما المنتج؟ وما المشكلة؟
- الأطروحة: ما الفكرة الهندسية الحاكمة؟
- رحلة القرارات: لماذا تغيرت المعمارية أو اختيرت التقنيات؟
- النموذج الذهني: مخطط واحد بصيغة SVG مضمّن يوضح انتقال البيانات، لا صور خارجية.
- تجربة المنتج: ماذا يرى المستخدم؟
- الغوص التقني: routing، rendering، state، data، performance، security، testing.
- الديمو الحي: شريحة فاصلة تشير إلى ملف السيناريو.
- المقايضات: ما الذي كسبناه وما الذي دفعناه ثمناً؟
- خارطة الطريق: أولويات مبنية على المخاطر، لا قائمة أمنيات.
- الخاتمة: جملة واحدة ثم أسئلة مفتوحة.

### شرح Hydration داخل `slides.html` (إلزامي عند وجود SSR أو SSG أو Client Components)

أضف شريحة واضحة، أو قسماً واضحاً في شريحة، يشرح العملية بالتفصيل لا بمجرد تعريف سطحي:

1. **Server render:** ما الذي يحسبه الخادم؟ وما الذي لا يستطيع الوصول إليه مثل `window` و`localStorage`؟
2. **HTML response:** كيف يصل HTML إلى المتصفح ويمكن عرضه قبل اكتمال JavaScript؟
3. **أول Client Render:** لماذا يجب أن ينتج العميل الشجرة الأولية نفسها التي أنتجها الخادم؟
4. **Hydration:** وضّح أنها عملية ربط React بالأشجار الموجودة وإضافة event handlers، وليست CSR يبدأ من DOM فارغ.
5. **Effects بعد commit:** وضّح أن `useEffect` يعمل بعد الالتزام بالشجرة، ويمكنه قراءة browser-only APIs ثم تحديث الحالة في تصيير لاحق.
6. **Mismatch:** اعرض مثالاً خاطئاً يقرأ preference من `localStorage` أثناء initial render، وبيّن كيف يمكن أن ينتج الخادم `dark` بينما ينتج أول client render `light`.
7. **النتيجة:** اشرح أن اختلاف markup قبل اكتمال hydration قد يؤدي إلى warning أو error أو إعادة معالجة للشجرة أو DOM غير متوقع، بحسب الإطار والحالة.
8. **النمط الصحيح:** اعرض النمط الفعلي في المشروع: default ثابت، قراءة بعد mount، و`hydrated` safeguard يمنع الكتابة فوق القيمة المحفوظة.
9. **فرق مهم:** ميّز بين mismatch أثناء أول render وبين state update بعد `useEffect`؛ الثاني متوقع إذا كان الأول متطابقاً.
10. **تطبيق فعلي:** اربط الشرح بـThemeContext أو hook تخزين موجود فعلاً. إن وُجد اختبار يثبت default قبل mount والاستعادة بعد mount وعدم clobber للبيانات فأشر إليه؛ وإلا فصرّح بغيابه.

### 2. `demo-script.md`

اكتب سيناريو ديمو حياً بلغة `[لغة العرض]`، مرقماً ومناسباً لمدة `[مدة العرض]`، ويتضمن:

- توزيع الوقت لكل مقطع.
- أوامر preflight الصحيحة من package.json.
- ماذا يفتح المتحدث في المتصفح، وماذا يقول في كل خطوة.
- إثباتاً للسلوك من الخارج ثم ربطه بملف وسطر من الداخل.
- تجربة routing أو URL state إن وجدت.
- تجربة الحالة المحلية وإعادة التحميل.
- تجربة البحث أو المعالجة الأساسية.
- تجربة API أو sync مع ذكر شروط البيئة المطلوبة.
- تجربة PWA أو offline من build production إذا كان service worker معطلاً في dev.
- شرح Hydration التفصيلي السابق، مع مثال mismatch ومثال safe pattern.
- خطة بديلة إذا غابت قاعدة البيانات أو متغيرات البيئة أو فشل أحد الطلبات.
- عبارات يجب قولها وعبارات يجب تجنبها حتى لا يبالغ العرض في الادعاء.

لا تكتب أوامر غير موجودة في scripts المشروع. إذا كان أمر ما يعتمد على Linux أو أداة خارجية، اذكر ذلك بوضوح وقدّم مساراً بديلاً.

### 3. `qa-guide.md`

أنشئ دليلاً بلغة `[لغة العرض]` لأسئلة الفريق، بين 25 و35 سؤالاً بحسب حجم المشروع. اجمع الأسئلة في مجموعات مثل:

- Architecture and system design.
- Rendering وSSR/SSG/CSR وHydration.
- Routing وstate management.
- Data pipeline وtext processing.
- API وdatabase وidentity.
- Performance وcaching وPWA.
- Security وthreat model.
- Testing وobservability.
- Trade-offs وroadmap.

لكل سؤال اكتب:

- إجابة قصيرة قابلة للنطق خلال 30-60 ثانية.
- مرجعاً دقيقاً للملف والسطر.
- المقايضة أو الحد المعروف.
- تحسيناً تالياً إذا كان مناسباً.

أضف أسئلة صريحة عن:

- الفرق بين SSR وCSR وHydration.
- سبب mismatch وكيفية منعه.
- ما الذي يحدث قبل وبعد `useEffect`.
- لماذا لا يجوز قراءة browser APIs أثناء render في تطبيق SSR.
- ما الذي اختُبر فعلاً وما الذي لم يُختبر.
- ما الادعاءات التي لا ينبغي للمقدم أن يقولها.

### 4. `README.md`

وثّق طريقة تشغيل الحزمة، اختصارات الشرائح، ترتيب الاستخدام، وفحص ما قبل العرض. اذكر أي شرط بيئي أو تحذير build حقيقي، ولا تضف ادعاءات غير قابلة للتحقق.

## مراجعة الجودة والتحقق

بعد إنشاء الملفات:

1. راجع كل claim وكل path وكل رقم مقابل المصدر الفعلي.
2. تحقق من أن كل code block في Markdown متوازن، وأن كل الروابط والمسارات موجودة.
3. تحقق من HTML وJavaScript المضمّن، وعدد الشرائح، والترقيم، والفهرس، واختصارات الكيبورد.
4. نفّذ smoke test للعرض إن أمكن باستخدام JSDOM أو HTML parser متاح في المشروع أصلاً (لا تضف dependency جديدة)، ثم اختبر next/previous وoverview وnotes.
5. شغّل أوامر التحقق الفعلية من scripts في package.json بمدير الحزم الصحيح للمشروع — اقرأه من lockfile الموجود (pnpm-lock.yaml أو package-lock.json أو yarn.lock أو bun.lockb). مثال لمشروع pnpm:

   ```bash
   pnpm test
   pnpm run lint
   pnpm run build
   ```

6. لا تشغّل build وtypecheck بالتوازي إذا كان typecheck يقرأ ملفات `.next` مولّدة؛ شغلهما بالتسلسل.
7. شغّل `git diff --check`، وراجع الملفات الجديدة والتغييرات غير المتعلقة بالمهمة.
8. لا تدّع نجاح test أو lint أو build دون عرض نتيجة حديثة بالأرقام والحالة.
9. لا تنشئ commit أو PR إلا إذا طلب المستخدم ذلك صراحة.

## شكل التقرير النهائي

أعد تقريراً مختصراً يتضمن:

- الملفات التي أنشأتها ومسؤولية كل ملف.
- أهم القرارات البصرية والتنظيمية.
- كيف تم شرح Hydration وSSR/CSR mismatch.
- أوامر التحقق ونتائجها الفعلية.
- أي warning أو limitation بقيت كما هي.
- اسم الفرع الحالي.
- تأكيد واضح إن لم يتم إنشاء commit أو PR.
````

## ملاحظة عملية

إذا كان الفريق سيختبر فهمك للكود، لا تحفظ الشرائح حرفياً. استخدمها كخريطة، وافتح المصدر عند السؤال، ثم أجب بهذا التسلسل: **نقطة الدخول → تدفق البيانات → سبب القرار → المقايضة → الاختبار**.
